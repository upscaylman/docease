{
  "name": "n8n-form",
  "version": "1.0.0",
  "description": "Formulaire de génération de documents pour n8n",
  "scripts": {
    "build": "node netlify-config.js && npm run build:css && npm run build:design-system && npm run build:react",
    "build:css": "tailwindcss -i ./src/input.css -o ./assets/css/tailwind.css --minify",
    "build:design-system": "node build-css.js",
    "build:react": "cd ../formulaire && npm install && npm run build",
    "prebuild": "echo 'Building with environment variables...'"
  },
  "keywords": [
    "n8n",
    "form",
    "netlify"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "tailwindcss": "^3.4.0"
  }
}
